{
  "name": "loop",
  "packageManager": "yarn@4.6.0",
  "workspaces": [
    "webapp",
    "contracts"
  ],
  "scripts": {
    "dev": "yarn workspace webapp dev",
    "db:generate": "yarn workspace webapp db:generate",
    "db:migrate:dev": "yarn workspace webapp db:migrate:dev",
    "db:migrate:prod": "yarn workspace webapp db:migrate:prod",
    "db:studio": "yarn workspace webapp db:studio",
    "contracts:compile": "yarn workspace contracts compile",
    "trigger:dev": "yarn workspace webapp trigger:dev",
    "trigger:deploy": "yarn workspace webapp trigger:deploy"
  }
}
